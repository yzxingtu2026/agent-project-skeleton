## 仓库信息

- **项目名称**：`<PROJECT_NAME>`
- **GitHub 仓库**：`https://github.com/<OWNER>/<REPO>`
- **默认分支**：`main`（建议受保护，仅接受 PR 合入）
- **Issue/PR 规范/开发规范**：参见 `.cursor/rules/*` 或 `.qoder/rules/*`

## 启动规范

- 开始处理任务前，先阅读 `.cursor/rules/*` 或 `.qoder/rules/*` 中的相关规范，尤其是主规则、Issue/PR 流程、分支策略与开发硬约束。
- 开始新 Issue 前先做冲突预检：查看 open PR，重叠较多或命中公共文件时先确认依赖/优先级。
- 若任务涉及新通用封装、统一组件、公共工具或跨模块约定，交付时必须同步沉淀规范。

## 当前使用者

- **姓名**：        <!-- 例：张三 -->
- **GitHub 用户名**： <!-- 例：zhangsan -->
- **角色**：         <!-- 从下方「团队角色」中选择一项，或写自定义角色 -->
- **常用语言**：      <!-- 例：简体中文 / English -->

## 使用者角色

### 项目负责人 / PM

- 职责：需求拆解、优先级排序、Issue 管理、PR 审查与合并决策。
- 权限：可创建/关闭 Issue、调整标签与里程碑、合并 PR。
- 协作偏好：先对齐范围和验收标准，再推动实现。

### 全栈开发

- 职责：跨前端、后端、数据与基础设施完成端到端交付。
- 权限：可修改运行时代码、测试、文档与配置。
- 协作偏好：保持模块边界清晰，公共能力优先抽象复用。

### 前端 / 客户端开发

- 职责：页面、组件、状态管理、交互与客户端服务调用。
- 权限：可修改客户端代码和对应测试。
- 协作偏好：复用统一组件与设计规范，避免业务页面重复实现公共能力。

### 后端开发

- 职责：API、服务层、领域模型、数据访问、任务队列与日志。
- 权限：可修改服务端代码、迁移、配置和对应测试。
- 协作偏好：接口契约清晰，核心流程有可追踪日志。

### QA / 测试

- 职责：功能验证、Bug 反馈、验收标准核查。
- 权限：默认只读代码；允许 checkout PR 分支并更新测试标签。
- 协作偏好：测试通过前不合并；失败时创建或关联 Bug Issue。

## 角色通用约束

- PR 目标分支为默认分支，通常是 `main`。
- 可交付工作必须关联 Issue；PR 使用 `Fixes #编号` 或 `Refs #编号`。
- 运行时行为变化需要测试；纯文档、规则、CI 配置可按项目规则直接合入。
- 测试通过收口遵循：先 approve，再添加 `tested-pass`。
